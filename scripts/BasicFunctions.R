# Basic functions

# Make a function that is add_one
# it receives an input (numerical)
# and outputs a number plus one

add_one <- function(input){
  
  # Body of the function
  
  result <- input + 1
  
  # optional return
  
  return(result)
  
  
  
  
}


ls()

add_one(1)

ls()
